#### 용어 정리
    예외 (Exception)
        : 개발자가 구현한 코드에서 발생, 발생할 상활을 미리 예측해 처리할 수 있도록 만든 안전 장치
          문법 오류로 인해 발생 X

    ICMP (Internet Control Message Protocol)
        : TCP/IP 계층 구조에서 IP 동작 과정에서의 전송 오류가 발생하는 경우에 대비해 오류 정보를 전송하는 목적으로 사용하는 프로토콜

    ARP (Address Resolution Protocol)
        : IP 네트워크상 IP 주소를 MAC 주소 (물리 주소)로 변환하는 프로토콜

    PPP (Point-to-Point Protocol)
        : 네트워크 분야에서 두 통신 노드 간 직접적인 연결 위해 일반적으로 사용되는 데이터 링크 프로토콜

    최적 적합 (Best Fit)
        : 가용 공간 중 가장 크기 비슷한 공간 선택해 프로세스 적재하는 방식
          (ex) 17KB 프로그램이 적재될 수 있는 곳은 20KB, 40KB인데, 17KB와 크기가 가장 비슷한 곳은 20KB임 / 내부 단편화 3KB
               내부 단편화
                    : 분할된 공간에 프로세스 적재 후 남은 공간

    커버로스 (Kerberos)
        : 컴퓨터 네트워크 인증 암호화 프로토콜


               

#### 임계 구역 접근 제어하는 상호배제 기법
    세마포어 (Semaphore)
        - 멀티 프로그래밍 환경에서 공유 자원에 대한 접근 제한하는 방법
        - P(임계 구역 들어가긴 전 수행), V(임계 구역에서 나올 때 수행) 연산 기반 구현
        - P or V 수행 동안 프로세스는 인터럽트 당하지 않게 되어 상호배제 구현 가능
| 연산 | 코드 | 설명 |
| --- | --- | --- |
| P 연산 | P(S) : while S <= 0 do skip; <br> S := S - 1; | 최초 S 값은 1 <br> S가 0이면 1이 될 때까지 대기 <br> S를 0으로 만들어 다른 프로세스가 들어오지 못하게 함 |
| V 연산 | V(S) : S := S + 1; | 현재 상태는 S가 0 <br> S를 1로 원위치시켜 해제함 |

<span style="color:blue"> 데커 알고리즘 (Dekker Algorithm) </span>
    : flag와 turn이라는 변수로 임계영역에 들어갈 프로세스 (or 스레드) 결정하는 기법
    
<span style="color:blue"> 랩포트 알고리즘 (Lamport Algorithm) </span>
    : 프로세스에게 고유한 번호 부여하고, 번호를 기준으로 우선순위 정해 우선순위 높은 프로세스 먼저 임계 구역에 진입하도록 구현하는 기법

<span style="color:blue"> 피터슨 알고리즘 (Peterson Algorithm) </span>
    - 공유 메모리 활용해 여러 개 프로세스가 하나의 자원 함께 사용할 때 문제 발생하지 않도록 해주는 기법
    - 데커 알고리즘과 유사하지만 다른 프로세스에게 진입 기회 양보한다는 차이 있음


#### bash 쉘 스크립트 제어문
    조건 : if
    반복 : for, while


#### PHP 연산자
    @ : 오류 억제 연산자
        오류 메시지 무시
    <> : 값이 다름 표현하는 연산자
    === : 값도 같고 타입도 같은지 확인하는 연산자


#### 프로토콜 포트 번호
| 프로토콜 | 내용 | 포트 번호 |
| --- | --- | --- |
| TFTP (Trivial File Transfer Protocol) | 파일 전송 위한 프로토콜이지만 <br> FTP보다 더 단순한 방식으로 파일 전송하고 데이터 전송 과정에서 데이터 손실될 수 있는 단점 가진 프로토콜 | UDP 69번 포트 |
| SNMP (Simple Network Management Protocol) | TCP / IP 네트워크 관리 프로토콜 <br> 라우터나 허브 등 네트워크 장치로부터 정보 수집 및 관리 <br> 정보를 네트워크 관리 시스템에 보내는 데 사용하는 인터넷 표준 프로토콜 | UDP 161번 포트 (Agent) <br> UDP 162번 포트 (Manager) |
| RPC (Remote Procedure Call) | 원격 프로시저 호출 <br> 별도 원격 제어 위한 코딩 없이 다른 주소 공간에서 함수 or 프로시저 실행할 수 있는 프로세스간 통신에 사용되는 프로토콜 | TCP / UDP 111번 포트 |
| TELNET | 인터넷 or 로컬 영역에서 네트워크 연결에 사용되는 네트워크 프로토콜 | TCP 23번 포트 |


#### 리눅스 명령어
    ls : 현재 디렉터리 내 파일 및 폴더들 목록 표시하는 명령어
    cat : 파일 내용 화면에 출력하는 명령어
    finger : 현재 시슽템에 로그인한 사용자 정보 (계정 정보) 확인하는 명령어
    mkdir : 디렉토리 생성 시 사용되는 명령어


#### IPv4
    : 20 Octet의 기본 헤더 부분과 옵션 부분으로 구성
      호스트 주소 자동으로 설정하며 유니캐스트 (Unicast) 지원
      클래스별로 네트워크와 호스트 주소 길이 다름
      주소 길이 32bit
      8비트씩 4부분으로 나뉜 10진수
      주소 개수 약 43억 개
      Best Effort 방식 (보장 곤란)
      IPSec 프로토콜 별도 설치

    
    
#### IPv6 
    : 40 OCtet의 고정된 길이 가짐
      주소 자동설정 (Auto Configuration) 기능 통해 손쉽게 이용자 단말을 네트워크에 접속시킬 수 O
      주소 길이 128bit
      16비트씩 8부분으로 나뉜 16진수
      주소 개수 2^128
      등급별, 서비스별 패킷 구분할 수 있어 품질 보장 용이
      보안과 인증 확장 헤더 사용함으로써 인터넷 계층 보안 기능 강화 (확장기능 통해 보안 기능 제공)  

#### OSI 7계층 데이터 링크 계층 프로토콜
    HDLC
    PPP
    Frame-Relay
    FDDI
    ATM
    LLC
    ALOHA


#### C언어 연산자
    || : 두 개 논릿값 중 하나 이상 참이면 참 (True) 반환, 그렇지 않으면 거짓 (False) 반환 (OR 연산)
    && : 두 개 논릿값이 모두 참이면 참 (True) 반환, 그렇지 않으면 거짓 (False) 반환 (AND 연산)
    != : 두 값 다른지 확인


#### 주요 예외
    RuntimeException
        : 오동작이나 결과에 악영향 미칠 수 있는 실행시간 동안 발생한 오류
    ArrayIndexOutOfBoundsException
        : 배열의 인덱스가 그 범위를 넘어서는 경우 발생하는 오류
    FileNotFoundException
        : 존재하지 않는 파일을 읽으려고 하는 경우 발생하는 오류


#### 모듈 (Module)
    다른 것들과 구별될 수 있는 독립적인 기능 가진 단위 (Unit)
    독립된 하나의 소프트웨어 or 하드웨어 단위 지징하는 용어
    독립적인 컴파일 가능, 재사용 가능
    다른 것들과 구분될 수 있도록 독립적인 기능 가져야 하고 유일한 이름 사용해야 함
    독립성 높이려면 모듈 결합도 약하게 하고 응집도 강하게 하며 모듈 크기 작게 만들어야 함
    다른 모듈에서 접근 가능해야 함


```C
// while 문은 반복문으로 while(조건식)에서 조건식이 참인 동안 계속 반복
// 조건식이 1일 경우 참이라고 판단하므로 while(1)일 때 무한히 반복
#include <stdio.h>
int main(int argc, char *argv[]) {
    int i = 0;
    while(1) {
        if(i==4) {
            break;
        }
        ++i;
    }
// i = 0인 상태에서 if(i==4)는 거짓이므로 break문 실행하지 않고, ++i에 의해 i는 1로 증가
// ...
// i = 4인 상태에서 if(i==4)는 참이므로 break문 만나 while 문 탈출

    printf("i = %d", i);
// i 값이 4인 상태에서 탈출했으므로 i 값인 4 출력 (i = 4)
    return 0;
}
```


#### 개발 도구
    빌드 도구
        - Ant
        - Maven
        - Gradle
    구현 도구 
        - Eclipse
        - IntelliJ
        - Spring Tool Stuite
        - NetBeans
        - Visual Studio
    테스트 도구
        - xUnit
        - PMD
        - Findbugs
        - cppCheck
        - SonarQube
    형상 관리 도구
        - CVS
        - SVN
        - Git


#### FIFO 페이지 교체 알고리즘 (First In First Out)
    시간상으로 메모리에 가장 먼저 들어온 페이지를 먼저 교체하는 알고리즘
    (ex) 3개 페이지 프레임 갖는 시스템에서 페이지 참조 순서가 1, 2, 1, 0, 4, 1, 3 일 경우 FIFO 알고리즘에 의한 페이지 교체의 경우 프레임의 최종 상태
            -> 프레임이 3개이므로 순서대로 1, 2 까지 넣고, 다음 1은 이미 할당되어 있으므로 패스
               다음 0을 세번째로 넣음 (1, 2, 0)
               4는 가장 먼저 들어온 1을 빼고 그 자리에 4를 넣음 (4, 2, 0)
               다시 1 들어가는데 두번째 들어왔던 2 빠지고 그 자리에 1 들어감 (4, 1, 0)
               마지막 3은 가장 마지막에 들어온 값인 0 자리로 채운다
                    => 4, 1, 3 
     

#### 연산자 우선순위
***증산시 관비 논삼대***
    증감 연산자
    산술 연산자
    시프트 연산자
    관계 연산자
    비트 연산자
    논리 연산자
    삼항 연산자
    대입 연산자
    (ex) 정수 a, b에 각각 1, 2가 저장되어 있을 때 연산 결과는?
            a < b + 2 && a << 1 <= b
            1 < 2 + 2 && 1 << 1 <= 2
                산술 연산자인 '+'가 가장 우선순위 높음
            1 < 4 && 1 << 1 <= 2
                시프트 연산자인 '<<'가 우선순위 높음
                시프트 연산자에서 1은 이진수로 바꾸면 1이고, 왼쪽으로 1칸 시프트 시키면 이진수로 10이 되는데, 이진수 10은 십진수로 2가 됨
            1 < 4 && 2 <= 2
                관계 연산자인 '<' 와 '<='가 우선순위 높음
                1 < 4는 참이므로 1이 되고, 2 <= 2도 참이므로 1이 됨
            1 && 1
                1은 참을 의미하므로 참과 참을 AND 연산하면 참이 되어 1이 됨
            => 1


#### UNIX 시스템 쉘 (Shell) 주요 기능
    사용자 명령을 해석하고 커널로 전달하는 기능 제공
        커널 : 쉘 프로그램 실행 위해 프로세스와 메모리 관리하는 기능 제공하는 것
    반복적인 명령을 프로그램으로 만드는 프로그래밍 기능 제공
    초기화 파일 이용해 사용자 환경 설정하는 기능 제공
              
